# TridentCash Landing (Next.js + Tailwind)

## Quickstart
```bash
npm i
npm run dev
# open http://localhost:3000
```

## Deploy (Vercel)
- Connect repo to Vercel → Deploy
- Add domains: `trident.cash`, `www.trident.cash`
- Cloudflare DNS (DNS only / grey cloud):
  - `@` A `76.76.21.21`
  - `www` CNAME `cname.vercel-dns.com`